# Vite Vue MDX marimo island

This page is MDX compiled by Vite with Vue 3 and `@mdx-js/vue`. The marimo fence below is rendered as a custom element.

```python marimo output=false
import marimo as mo
```

```python marimo
slider = mo.ui.slider(1, 10, label="items")
slider
```

```python marimo
mo.md(f"The slider is set to **{slider.value}**.")
```
